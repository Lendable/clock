name: Sloth

on:
  pull_request:
  merge_group:

jobs:
  sloth:
    runs-on: [ubuntu-slim]
    steps:
      - name: Sloth
        uses: lendable/sloth@v1
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          ignored: |
            Validate PR title
